List of properties from EntityVersion
=====================================
        #. timestamp(NUMBER(Date representation wrapped))
        #. entity_class(STRING<ENTITY_CLASS_NAME>)
        #. entity_id(NUMBER)
        #. entity(NUMBER)
        #. id(NUMBER)
